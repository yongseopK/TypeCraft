<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.codeman.typecraft.question.repository.QuestionRepository">

    <select id="findAllActive" resultType="Question">
        SELECT
               id
             , axis
             , direction
             , text
             , category
             , display_order
             , is_active
             , created_at
             , updated_at
          FROM questions
         WHERE is_active = TRUE
         ORDER BY display_order ASC
    </select>

</mapper>
